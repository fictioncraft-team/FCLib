{ //this set of bracket is the map bracket. When a JsonConfig object is created
  "configs": { //this is the start of the map. The map is called configs
    "test": [ //the first string value in the map ("test") and the object is a SimpleConfigObject
      { //start of SimpleConfigObject
        "target": { // start of SimpleItemProvider in SimpleConfigObject
          "amount": 1,
          "nbt": "",
          "name": "minecraft:carrot",
          "isTag": false,
          "type": "ITEM"
        } //end of SimpleSimpleItemProvider
      } //end of SimpleConfigObject
    ]
  }
}